import styled from "styled-components";
import { RouterTabs } from "../../../../router/components/router-tabs/router-tabs";

export const HomeTabsRoot = styled(RouterTabs)(() => ({
    padding: "0 2rem",
}));
