import { Tabs } from "@frontend/design-system-react/tabs";
import { RouterTabsProps } from "./router-tabs.types";

export const RouterTabs = (props: RouterTabsProps): JSX.Element => <Tabs {...props} />;
